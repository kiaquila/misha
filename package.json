{
  "name": "misha",
  "private": true,
  "type": "module",
  "description": "Mikhail Orlov — one-page CV and portfolio",
  "scripts": {
    "check": "node scripts/check-repository.mjs && npm --prefix website run check",
    "check:repository": "node scripts/check-repository.mjs",
    "test": "node --test \"tests/*.test.mjs\""
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
